def delete_memory():
    try:
        if os.path.exists(INDEX_PATH):
            os.remove(INDEX_PATH)
        if os.path.exists(META_PATH):
            os.remove(META_PATH)
        # reset in-memory
        global _metadata, _next_id, _index
        _metadata = {}
        _next_id = 0
        _index = faiss.IndexFlatL2(_EMBED_DIM)
        return True
    except Exception:
        return Falseÿÿÿÿ